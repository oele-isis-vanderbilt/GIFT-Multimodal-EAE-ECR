# macOS
.DS_Store

# Editor / agent local config
.claude/
.vscode/
.idea/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# Node / Rust / Tauri build artifacts (regenerated on first launch)
node_modules/
dist/
dist-ssr/
.vite/
.build/
**/src-tauri/target/
**/src-tauri/gen/schemas/
.node/

# Data / weights / scratch — never shared; supplied separately or regenerated
/output/*
/models/*
/work_dirs/
/tests/

# Keep the input folder present but empty when shared
/input/*

# Developer-only doc
DESCRIPTION.md

# Keep the .gitkeep placeholders that hold empty dirs in git
!**/.gitkeep

# IEEE TLT paper workspace (validation study, inter-team analysis, paper
# figures) — data/results, not repo code; never shared via git
/Results-for-TLT/

# Local export/test artifacts produced in the pose-backends worktree
/export_artifacts/

# Stray run logs at repo root (bench/debug runs)
/*.log

# /trackdiag/ — retained mocap capture data only (raw QTM archive + curated study inputs).
# The tracking-diagnostics paper project that lived here was discontinued 2026-08-21 and
# its code, results and git history were deleted. What remains is human-subjects data and
# must never enter this history. See trackdiag/README.md.
/trackdiag/
